package org.scopetext.model.dao;

/**
 * Created by john.qualls on 8/9/2016.
 */
public class ResponseDAO {
    // TODO - implement method to create table
/*    public static final String SQL_CREATE_TABLE =
            "CREATE TABLE " + ResponseSchema.TABLE_NAME + " (\n" +
                    ResponseSchema._ID + " INTEGER PRIMARY KEY NOT NULL,\n" +
                    ResponseSchema.ACTION_CD + " VARCHAR(1) NOT NULL CHECK(IN ('C', 'R', 'U', 'D')\n" +
                    ResponseSchema.EXT_APP_CD + " VARCHAR(1) NOT NULL CHECK(IN ('A'))\n";*/
}
