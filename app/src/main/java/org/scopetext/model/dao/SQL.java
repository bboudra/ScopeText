package org.scopetext.model.dao;

/**
 * Used in SQLTask to determine which SQL statement should be executed. Created by john.qualls on
 * 1/12/2017.
 */

public enum SQL {
    SELECT_ALL_SCOPETEXTS_CONTACTS;
}
